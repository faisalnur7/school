@extends('layouts.master')

@section('contents')
<div class="container-fluid py-4 admit-cards-page">
    <div class="page-hero mb-4">
        <div><span class="eyebrow"><i class="fas fa-id-card mr-2"></i>Admission management</span><h1>Admit cards</h1><p>Generate and print admit cards for paid applicants.</p></div>
        <div class="hero-count"><strong>{{ $applications->total() }}</strong><span>matching applicants</span></div>
    </div>

    <div class="filter-panel mb-4">
        <div class="filter-title"><div><h4>Find applicants</h4><p>Filter by examination, academic session, or class.</p></div><i class="fas fa-sliders-h"></i></div>
        <form method="GET" action="{{ route('admissions.admit-cards') }}" class="form-row align-items-end">
            <div class="form-group col-lg-4 mb-3 mb-lg-0"><label for="admitExam">Exam</label><select id="admitExam" name="exam_id" class="form-control"><option value="">All exams</option>@foreach($exams as $exam)<option value="{{ $exam->id }}" @selected($examId == $exam->id)>{{ $exam->name }}</option>@endforeach</select></div>
            <div class="form-group col-lg-3 mb-3 mb-lg-0"><label for="admitSession">Academic session</label><select id="admitSession" name="academic_session_id" class="form-control"><option value="">All sessions</option>@foreach($sessions as $session)<option value="{{ $session->id }}" @selected($sessionId == $session->id)>{{ $session->name_en }}</option>@endforeach</select></div>
            <div class="form-group col-lg-3 mb-3 mb-lg-0"><label for="admitClass">Class</label><select id="admitClass" name="school_class_id" class="form-control"><option value="">All classes</option>@foreach($classes as $class)<option value="{{ $class->id }}" @selected($classId == $class->id)>{{ $class->name_en }}</option>@endforeach</select></div>
            <div class="form-group col-lg-2 mb-0 d-flex"><button type="submit" class="btn btn-primary flex-grow-1"><i class="fas fa-search mr-1"></i>Filter</button><a href="{{ route('admissions.admit-cards') }}" class="btn btn-light ml-2" title="Clear filters"><i class="fas fa-undo"></i></a></div>
        </form>
    </div>

    <div class="list-panel">
        <div class="list-heading"><div><h4>Eligible applicants</h4><p>Only applications with verified payment are shown.</p></div><span class="result-count">{{ $applications->count() }} on this page</span></div>
        <div class="table-responsive"><table class="table applicant-table mb-0"><thead><tr><th>Applicant</th><th>Exam & session</th><th>Class</th><th>Admit card</th><th class="text-right">Action</th></tr></thead><tbody>
            @forelse($applications as $application)
                <tr><td><div class="applicant-cell"><span class="applicant-avatar">{{ strtoupper(substr($application->applicant_data['full_name_en'] ?? $application->full_name_en ?? '?', 0, 1)) }}</span><div><strong>{{ $application->applicant_data['full_name_en'] ?? $application->full_name_en ?? '-' }}</strong><small>{{ $application->application_number }}</small></div></div></td><td><strong>{{ $application->exam?->name ?? '-' }}</strong><small>{{ $application->academicSession?->name_en ?? '-' }}</small></td><td><span class="class-badge">{{ $application->schoolClass?->name_en ?? '-' }}</span></td><td>@if($application->admitCard)<span class="status-badge status-ready"><i class="fas fa-check-circle mr-1"></i>{{ $application->admitCard->admit_card_number }}</span>@else<span class="status-badge status-pending"><i class="fas fa-clock mr-1"></i>Not generated</span>@endif</td><td class="text-right text-nowrap"><form class="d-inline" method="POST" action="{{ route('admissions.admit-cards.generate', $application) }}">@csrf<button class="btn btn-sm {{ $application->admitCard ? 'btn-outline-primary' : 'btn-primary' }}">{{ $application->admitCard ? 'Regenerate' : 'Generate' }}</button></form>@if($application->admitCard)<a class="btn btn-sm btn-outline-dark ml-1" href="{{ route('public.admission.admit-card', $application) }}"><i class="fas fa-download mr-1"></i>Print</a>@endif</td></tr>
            @empty <tr><td colspan="5" class="empty-state"><i class="fas fa-id-card"></i><strong>No eligible applicants found</strong><span>Try changing the filters or verify an application payment first.</span></td></tr>@endforelse
        </tbody></table></div><div class="list-footer">{{ $applications->links() }}</div>
    </div>
</div>

<style>
    .admit-cards-page { color:#172033; }.page-hero { align-items:center; background:linear-gradient(120deg,#102c46,#175e69); border-radius:15px; box-shadow:0 10px 24px rgba(16,44,70,.14); color:#fff; display:flex; justify-content:space-between; min-height:145px; overflow:hidden; padding:28px 32px; position:relative; }.page-hero:after { border:1px solid rgba(255,255,255,.14); border-radius:50%; content:''; height:250px; position:absolute; right:-55px; top:-140px; width:250px; }.page-hero > div { position:relative; z-index:1; }.eyebrow { color:#a7f3d0; font-size:11px; font-weight:700; letter-spacing:1.2px; text-transform:uppercase; }.page-hero h1 { font-size:29px; margin:9px 0 5px; }.page-hero p { color:rgba(255,255,255,.75); font-size:13px; margin:0; }.hero-count { border-left:1px solid rgba(255,255,255,.25); padding-left:28px; text-align:right; }.hero-count strong { display:block; font-size:31px; line-height:1; }.hero-count span { color:rgba(255,255,255,.7); font-size:11px; }.filter-panel,.list-panel { background:#fff; border:1px solid #e5ebf2; border-radius:14px; box-shadow:0 4px 13px rgba(30,41,59,.05); }.filter-panel { padding:20px; }.filter-title,.list-heading { align-items:flex-start; display:flex; justify-content:space-between; margin-bottom:18px; }.filter-title h4,.list-heading h4 { font-size:16px; font-weight:700; margin:0; }.filter-title p,.list-heading p { color:#8492a6; font-size:11px; margin:4px 0 0; }.filter-title > i { color:#b7c4d3; font-size:17px; }.filter-panel label { color:#526174; font-size:11px; font-weight:600; margin-bottom:6px; }.filter-panel .form-control { border-color:#d9e2ec; border-radius:7px; font-size:12px; height:40px; }.list-heading { border-bottom:1px solid #edf1f5; margin:0; padding:18px 20px 15px; }.result-count { background:#f1f5f9; border-radius:20px; color:#64748b; font-size:10px; padding:6px 10px; }.applicant-table thead th { background:#f8fafc; border-bottom:1px solid #e8edf3; color:#718096; font-size:10px; letter-spacing:.3px; padding:12px 20px; text-transform:uppercase; white-space:nowrap; }.applicant-table tbody td { border-top:1px solid #f0f3f6; color:#526174; font-size:12px; padding:14px 20px; vertical-align:middle; }.applicant-cell { align-items:center; display:flex; }.applicant-avatar { align-items:center; background:#e8f7f5; border-radius:9px; color:#0f766e; display:inline-flex; font-size:12px; font-weight:700; height:34px; justify-content:center; margin-right:10px; width:34px; }.applicant-table td strong { color:#26364a; display:block; font-weight:600; }.applicant-table td small { color:#94a3b8; display:block; font-size:10px; margin-top:3px; }.class-badge,.status-badge { border-radius:15px; display:inline-block; font-size:10px; padding:5px 9px; }.class-badge { background:#eef5ff; color:#2563eb; }.status-ready { background:#eaf8ef; color:#16803c; }.status-pending { background:#fff7e5; color:#b45309; }.empty-state { color:#94a3b8 !important; padding:55px 20px !important; text-align:center; }.empty-state i,.empty-state strong,.empty-state span { display:block; }.empty-state i { color:#cbd5e1; font-size:30px; margin-bottom:12px; }.empty-state strong { color:#526174; font-size:14px; }.empty-state span { font-size:11px; margin-top:4px; }.list-footer { padding:14px 20px; }.list-footer nav { margin:0; }.list-footer .pagination { margin-bottom:0; }
    @media (max-width:767.98px) { .page-hero { align-items:flex-start; display:block; padding:24px; }.hero-count { border-left:0; border-top:1px solid rgba(255,255,255,.25); margin-top:20px; padding:15px 0 0; text-align:left; }.filter-title,.list-heading { display:block; }.result-count { display:inline-block; margin-top:10px; }.applicant-table tbody td,.applicant-table thead th { padding-left:12px; padding-right:12px; } }
</style>
@endsection
