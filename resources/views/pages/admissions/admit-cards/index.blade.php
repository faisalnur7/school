@extends('layouts.master')
@section('contents')
<div class="container-fluid py-3"><h2>Admission Admit Cards</h2><p class="text-muted">Only paid applications are eligible.</p><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Application</th><th>Applicant</th><th>Class</th><th>Card</th><th>Actions</th></tr></thead><tbody>@foreach($applications as $application)<tr><td>{{ $application->application_number }}</td><td>{{ $application->applicant_data['full_name_en'] ?? '-' }}</td><td>{{ $application->schoolClass?->name_en }}</td><td>{{ $application->admitCard?->admit_card_number ?? 'Not generated' }}</td><td><form class="d-inline" method="POST" action="{{ route('admissions.admit-cards.generate', $application) }}">@csrf<button class="btn btn-sm btn-primary">Generate</button></form>@if($application->admitCard)<a class="btn btn-sm btn-outline-dark" href="{{ route('public.admission.admit-card', $application) }}">Download</a>@endif</td></tr>@endforeach</tbody></table></div><div class="p-3">{{ $applications->links() }}</div></div></div>
@endsection
