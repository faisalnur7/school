---
deployment:
  tasks:
    - export DEPLOYPATH="${HOME}/public_html/"
    - /bin/cp -R app bootstrap config database resources routes storage vendor public/. artisan composer.json composer.lock index.php .htaccess package.json package-lock.json vite.config.js tailwind.config.js postcss.config.js "$DEPLOYPATH"
